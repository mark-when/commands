To contribute a command to this list, make a new folder with a descriptive name for the command. Add a README that explains what the command does, the actual script in a `script.js` file, and a simple `package.json` (with `name` and `version` at a minimum).